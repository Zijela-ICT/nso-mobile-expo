import React, { useEffect } from 'react';
import { StatusBar, View } from 'react-native';

const StatusBarConfig: React.FC = () => {
    return (
        <StatusBar
            backgroundColor='#fff'
            barStyle='dark-content'
        />
    );
};

export default StatusBarConfig;
